# Migration `20190720151519-ondelete-round2`

This migration has been generated by Matt Terski at 7/20/2019, 3:15:19 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190720150908-ondelete..20190720151519-ondelete-round2
--- datamodel.dml
+++ datamodel.dml
@@ -28,14 +28,14 @@
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   version   Int
   data      String
-  aggregate Aggregate @relation(onDelete: CASCADE)
+  aggregate Aggregate
 }
 model Aggregate {
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   type      String
   version   Int
-  events    Event[]
+  events    Event[]  @relation(onDelete: CASCADE)
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190720151519-ondelete-round2)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190720151519-ondelete-round2'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
