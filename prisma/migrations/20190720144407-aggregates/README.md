# Migration `20190720144407-aggregates`

This migration has been generated by Matt Terski at 7/20/2019, 2:44:07 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Event"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"version" INTEGER NOT NULL DEFAULT 0 ,"data" TEXT NOT NULL DEFAULT '' ,"aggregate" TEXT NOT NULL  REFERENCES Aggregate(id),PRIMARY KEY ("id"));

CREATE TABLE "lift"."Aggregate"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"type" TEXT NOT NULL DEFAULT '' ,"version" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Event.id._UNIQUE" ON "Event"("id")

CREATE UNIQUE INDEX "lift"."Aggregate.id._UNIQUE" ON "Aggregate"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190624162221-init..20190720144407-aggregates
--- datamodel.dml
+++ datamodel.dml
@@ -1,13 +1,11 @@
 datasource db {
   provider = "sqlite"
-  url      = "file:dev.db"
-  default  = true
+  url      = "file:./dev.db"
 }
 generator photon {
   provider = "photonjs"
-  output   = "../node_modules/@generated/photon"
 }
 model User {
   id    String  @default(cuid()) @id @unique
@@ -23,5 +21,21 @@
   published Boolean
   title     String
   content   String?
   author    User?
-}
+}
+
+model Event {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  version   Int
+  data      String
+  aggregate Aggregate
+}
+
+model Aggregate {
+  id        String   @default(cuid()) @id @unique
+  createdAt DateTime @default(now())
+  type      String
+  version   Int
+  events    Event[] @relation(onDelete: CASCADE)
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190720144407-aggregates)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190720144407-aggregates'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
