# Migration `20190721143814-rename-event`

This migration has been generated by Matt Terski at 7/21/2019, 2:38:14 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."Event";

CREATE TABLE "lift"."AggregateEvent"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"type" TEXT NOT NULL DEFAULT '' ,"data" TEXT NOT NULL DEFAULT '' ,"aggregate" TEXT NOT NULL  REFERENCES Aggregate(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."AggregateEvent.id._UNIQUE" ON "AggregateEvent"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190721143151-simplify-subscriptions..20190721143814-rename-event
--- datamodel.dml
+++ datamodel.dml
@@ -23,9 +23,9 @@
   content   String?
   author    User?
 }
-model Event {
+model AggregateEvent {
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   type      String
   data      String
@@ -35,9 +35,9 @@
 model Aggregate {
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   type      String
-  events    Event[]  @relation(onDelete: CASCADE)
+  events    AggregateEvent[]  @relation(onDelete: CASCADE)
 }
 model Subscription {
   id              String   @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190721143814-rename-event)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190721143814-rename-event'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
